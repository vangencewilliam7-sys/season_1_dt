/**
 * Pre-Sales Skills Components — Dev D Workspace
 * ================================================
 * This directory contains all Pre-Sales-specific UI components
 * for the Skills page.
 *
 * Dev D (Pre-Sales) owns this directory.
 *
 * ADDING A NEW PRE-SALES SKILL UI:
 *   1. Create a new .js component file here
 *   2. Import it in the Skills page or SkillSandbox
 *   3. Wire it to the Pre-Sales adapter's skill list
 *
 * Planned components:
 *   - TechStackInferencePanel.js — UI for SKL_TECH_STACK_INFERENCE
 *   - DiscoveryBriefPanel.js     — UI for SKL_DISCOVERY_BRIEF
 *   - ReferenceMatchPanel.js     — UI for SKL_REFERENCE_MATCH
 */

export { default as PreSalesSkillIndex } from './PreSalesSkillIndex';
